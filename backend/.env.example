# ---- RoamWise Backend Configuration ----

# Server Port
PORT=3000

# JWT Secret (CHANGE THIS IN PRODUCTION!)
# Generate a secure random string: openssl rand -base64 32
JWT_SECRET=your-secret-key-here-change-in-production

# ---- OSRM Configuration ----

# OSRM Server URL
# Local development: http://localhost:5000
# Production: https://your-osrm-instance.run.app
OSRM_URL=http://localhost:5000

# Route request timeout (milliseconds)
# Default: 12000ms (12 seconds)
ROUTE_TIMEOUT_MS=12000

# Route cache TTL (milliseconds)
# Default: 300000ms (5 minutes)
ROUTE_CACHE_TTL_MS=300000

# Route cache max entries
# Default: 1000 routes
ROUTE_CACHE_MAX=1000
